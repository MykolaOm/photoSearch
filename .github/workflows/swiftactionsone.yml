name: build only

on: [push]

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: xcodebuild -scheme searchPhoto -workspace searchPhoto.xcworkspace/ test
        # xcodebuild build-for-testing -workspace searchPhoto.xcworkspace -scheme searchPhoto -destination generic/platform=iOS
      # run: xcodebuild -workspace searchPhoto.xcworkspace -scheme searchPhoto
    # - name: Run tests
    #   run: swift test -v
